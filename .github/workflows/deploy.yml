# Disabled until we're ready to actually deploy to production.
# Was auto-running (and failing) on every push to main since the FTP
# secrets were never set. Uncomment when going live.
#
# name: Deploy to WebHS
#
# on:
#   push:
#     branches: [main]
#
# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v7
#
#       - name: Set up Node
#         uses: actions/setup-node@v7
#         with:
#           node-version: 22
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Build site
#         run: npm run build
#
#       - name: Deploy over FTP
#         uses: SamKirkland/FTP-Deploy-Action@v4.4.0
#         with:
#           server: ${{ secrets.WEBHS_FTP_HOST }}
#           username: ${{ secrets.WEBHS_FTP_USERNAME }}
#           password: ${{ secrets.WEBHS_FTP_PASSWORD }}
#           local-dir: ./dist/
#           server-dir: ./public_html/
